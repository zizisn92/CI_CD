
# Base image
.image-docker-with-bash:
  image: dockerhub.dttt.vn/docker-with-bash:0.0.2
.image-dotnet-core:
  image: mcr.microsoft.com/dotnet/sdk:5.0
.image-base-angular:
  image: node:14
  
# # Common
# .common-build-and-deploy-app:
#   script: chmod +x $APP_PATH/deploy.ci

# # Rules
# .if-dev:
#   rules:
#     - if: '$CI_COMMIT_BRANCH != "dev"'
#       when: never
# .if-test:
#   rules:
#     - if: '$CI_COMMIT_BRANCH != "test"'
#       when: never
# .if-stagging:
#   rules:
#     - if: '$CI_COMMIT_BRANCH != "stagging"'
#       when: never
# .if-prod:
#   rules:
#     - if: '$CI_COMMIT_BRANCH != "prod"'
#       when: never
# .if-dev-u202001:
#   rules:
#     - if: '$CI_COMMIT_BRANCH != "dev-u202001"'
#       when: never
# .if-hotfix:
#   rules:
#     - if: "$CI_COMMIT_BRANCH !~ /^hotfix-/"
#       when: never
